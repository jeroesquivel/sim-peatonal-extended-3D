package ar.edu.itba.simped.simulation;

import ar.edu.itba.simped.core.ports.SimulationDriver;

/**
 * STUB - implementación del SimulationLoop pendiente.
 *
 * <p>Quien tome la implementación de este módulo debe consumir {@code dt},
 * {@code dtout} y {@code t_total} desde {@code SIM_PARAMS.csv}, ejecutar
 * {@code Environment.init()} (I4), iterar disparando {@code Agent.step(dt)}
 * (I3) y muestrear a {@link ar.edu.itba.simped.core.ports.OutputSink}
 * cada {@code dtout} (I5).</p>
 */
public final class StubSimulationDriver implements SimulationDriver {

    @Override
    public void run() {
        // TODO: implementar el loop según contract §2.
    }
}
